import { theme } from '@/src/stitches';
import { colors } from '@/src/variables';
import { ColorPreview } from '@/app/components';

### Scale

<div>
	{Object.keys(colors.scales).map((colorName) => {
		const { value, token } = theme.colors[colorName];
		console.log(theme.colors);
		return (
			<ColorPreview
				key={token}
				value={value}
				token={token}
			/>
		);
	})}
</div>

### Backgrounds

<div>
	{Object.keys(colors.backgrounds).map((colorName) => {
		const { value, token } = theme.colors[colorName];
		return (
			<ColorPreview
				key={token}
				value={value}
				token={token}
			/>
		);
	})}
</div>

### Contents

<div>
	{Object.keys(colors.contents).map((colorName) => {
		const { value, token } = theme.colors[colorName];
		return (
			<ColorPreview
				key={token}
				value={value}
				token={token}
			/>
		);
	})}
</div>

### Borders

<div>
	{Object.keys(colors.borders).map((colorName) => {
		const { value, token } = theme.colors[colorName];
		return (
			<ColorPreview
				key={token}
				value={value}
				token={token}
			/>
		);
	})}
</div>
