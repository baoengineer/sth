import { theme } from '@/src/stitches';
import { vars } from '@/src/variables';
import { SpacingPreview } from '@/app/components';

### 8px grid

<div>
	{Object.keys(vars.spaces).map((spaceName) => {
		const { token, value } = theme.space[spaceName];
		return (
			<SpacingPreview
				token={token}
				value={value}
			/>
		);
	})}
</div>
